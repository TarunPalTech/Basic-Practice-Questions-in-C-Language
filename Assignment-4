                    Assignment – 4 
            More on Operators in C Language
1. Write a program to input a three digit number and display the sum of digits.
#include<stdio.h>
int main(){
    int x,sum;
    printf("Enter a three digit number:");
    scanf("%d",&x);
    sum=x%10+x/10%10+x/100;
    printf("Sum is %d.",sum);
    return 0;
}
2. Write a program to find ASCII code of the character ‘+’.
#include<stdio.h>
int main(){
    char m='+';
    printf("ASCII code is %d.",m);
    return 0;
}

3. Write a program to print size of an int, a float, a char and a double type variable.
#include<stdio.h>
int main(){
    int x,a,b,c,d;
    char m;
    float k;
    double d1;
    a=sizeof(x);
    b=sizeof(m);
    c=sizeof(k);
    d=sizeof(d1);
    printf("Size of int is - %d.\n",a);
    printf("Size of char is - %d.\n",b);
    printf("Size of float is - %d.\n",c);
    printf("Size of double is - %d.",d);
    return 0;
}
4. Write a program to make the last digit of a number stored in a variable as zero (Example – if x=2345 then make it x=2340)
#include<stdio.h>
int main(){
    int x,y;
    printf("Enter a number:");
    scanf("%d",&x);
    y=x/10*10;
    printf("Now number is:%d",y);
    return 0;
}
5. Write a program to input a number from the user and also input a digit. Append a digit in the number and print the resulting number. (Example – number=234 and digit=9 then the resulting number is 2349)
#include<stdio.h>
int main(){
    int x,y,z;
    printf("Enter a number:");
    scanf("%d",&x);
    printf("Enter a digit:");
    scanf("%d",&y);
    z=x*10+y;
    printf("Now number is:%d",z);
    return 0;
}
6. Assume price of 1 USD is INR 84.23. Write a program to take the amount in INR and convert it into USD.
#include<stdio.h>
int main(){
    float x,y=84.23,z;
    printf("Enter amount in INR:");
    scanf("%f",&x);
    z=x/y;
    printf("Amount is %f.",z);
    return 0;
}
7. Write a program to take a three digit number from the user and rotate its digits by one position towards the right.
#include<stdio.h>
int main(){
    int x,y;
    printf("Enter a three digit number:");
    scanf("%d",&x);
    y=x%10*100+x/10%10*10+x/100;
    printf("Now number is %d.",y);
    return 0;
}
8. What will be the value stored in the variable x after executing following statement.
	x=10>8>4;
#include<stdio.h>
int main(){
    int x;
    x=10>8>4;
    printf("Stored value is %d.",x);
    return 0;
}
Output: Stored value is zero!
9. What will be the value stored in the variable x after executing following statement.
	x=!2>-2;
#include<stdio.h>
int main(){
    int x;
    x=!2>-2;
    printf("Stored value is %d.",x);
    return 0;
}
Output: Stored value is 1.
10. What will be the value stored in the variable x after executing following statement.
	x=3<0&&5>0;
#include<stdio.h>
int main(){
    int x;
    x=3<0&&5>0;
    printf("Stored value is %d.",x);
    return 0;
}
Output: Stored value is 0.
