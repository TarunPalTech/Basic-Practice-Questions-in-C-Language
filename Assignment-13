                    Assignment – 13
                  Switch Case Control
1. Write a program which takes the month number as an input and display number of days in that month.
#include<stdio.h>
int main(){
    int n;
    printf("Enter a number:");
    scanf("%d",&n);
    switch(n)
    {
        case 1:
        case 3:
        case 5:
        case 7:
        case 8:
        case 10:
        case 12:
            printf("Number of days are 31.");
            break;
        case 2:
            printf("Number of days are 28.");
            break;
        case 4:
        case 6:
        case 9:
        case 11:
            printf("Number of days are 30.");
    }
}


2. Write a menu driven program with the following options:
1.	Addition
2.	Subtraction
3.	Multiplication
4.	Division
5.	Exit
#include<stdio.h>
int main(){
    int k=1;
    while(k){
        float a,b;
        int n;
        char m;
        printf("There are multiple options You can select any of them as per your requirement!\n");
        printf("Press one for Addition,\n");
        printf("Press 2 for Subtraction.\n");
        printf("Press 3 for Multiplication:\n");
        printf("Press 4 for Division.\n");
        printf("Press 5 to exit.\n");
        printf("Select an option:");
        scanf("%d",&n);
        switch(n){
            case 1:
                printf("Enter first Number:");
                scanf("%f",&a);
                printf("Enter second number:");
                scanf("%f",&b);
                printf("Addition is %.2f.\n",a+b);
                break;
            case 2:
                printf("Enter first Number:");
                scanf("%f",&a);
                printf("Enter second number:");
                scanf("%f",&b);
                printf("Subtraction is %.2f.\n",a-b);
                break;
            case 3:
                printf("Enter first Number:");
                scanf("%f",&a);
                printf("Enter second number:");
                scanf("%f",&b);
                printf("Multiplication is %.2f.\n",a*b);
                break;
            case 4:
                printf("Enter first Number:");
                scanf("%f",&a);
                printf("Enter second number:");
                scanf("%f",&b);
                printf("Division is %.2f.\n",a/b);
                break;
            case 5:
                printf("Now you are out!");
                k=0;
        }
    }
}

3. Write a program which takes the day number of a week and displays a unique greeting message for the day.
#include<stdio.h>
int main(){
    int x;
    printf("Enter a number:");
    scanf("%d",&x);
    switch(x){
        case 1:
            printf("Good morning! Rise and shine—it’s a brand new day!");
            break;
        case 2:
            printf("Hello there! How can I brighten your day today?");
            break;
        case 3:
            printf("Hey, friend! Sending positive vibes your way.");
            break;
        case 4:
            printf("Hi! Hope your day is as wonderful as you are.");
            break;
        case 5:
            printf("Greetings! May your day be filled with joy and laughter.");
            break;
        case 6:
            printf("Hola! Wishing you a fantastic day ahead.");
            break;
        case 7:
            printf("Namaste! Embrace the day with gratitude and positivity. ");
    }
    return 0;
}
4. Write a menu driven program with the following options:
1.	Check whether a given set of three numbers are lengths of an isosceles triangle or not. 
2.	Check whether a given set of three numbers are lengths of sides of a right angled triangle or not.
3.	Check whether a given set of three numbers are equilateral or not.
4.	Exit
#include<stdio.h>
int main(){
    int k=1;
    while(k){
        int a,b,c,n;
        printf("Please select an option:\n");
        printf("2 for isosceles triangle:\n");
        printf("2 for right angled triangle:\n");
        printf("3 for equilateral triangle:\n");
        printf("4 for exit:\n");
        scanf("%d",&n);
        switch(n){
            case 1:
                printf("Enter a side:");
                scanf("%d",&a);
                printf("Enter a side:");
                scanf("%d",&b);
                printf("Enter a side:");
                scanf("%d",&c);
                if(a==b && b==c)
                    printf("Triangle is not isosceles.\n");
                else if(a==b || b==c ||a==c)
                    printf("Triangle is an isosceles triangle.\n");
                else
                    printf("Triangle isn't an isosceles triangle.\n");
                break;
            case 2:
                printf("Enter a side:");
                scanf("%d",&a);
                printf("Enter a side:");
                scanf("%d",&b);
                printf("Enter a side:");
                scanf("%d",&c);
                if((a*a)+(b*b)==(c*c) || (b*b)+(c*c)==(a*a) || (a*a)+(c*c)==(b*b))
                    printf("Triangle is a right angled triangle.\n");
                else
                    printf("Triangle isn't a right angled triangle.\n");
                break;
            case 3:
                printf("Enter a side:");
                scanf("%d",&a);
                printf("Enter a side:");
                scanf("%d",&b);
                printf("Enter a side:");
                scanf("%d",&c);
                if(a==b && b==c)
                    printf("Triangle is an equilateral triangle.\n");
                else
                    printf("Triangle isn't an equilateral triangle.\n");
            case 4:
                k=0;
        }
    }
    return 0;
}
5. Convert the following if-else-if construct into switch case:
	If(var==1)
			printf(“good”);
		else if(var==2)
			printf(“better”);
		else if(var==3)
			printf(“best);
		else		
			printf(“Invalid”);
#include<stdio.h>
int main(){
    int n;
    printf("Enter a number:");
    scanf("%d",&n);
    switch(n){
        case 1:
            printf("good");
            break;
        case 2:
            printf("better");
            break;
        case 3:
            printf("best");
            break;
        default:
            printf("Invalid");
    }
    return 0;
}
6. Program to find and display grade obtained by a student in a test. Grading specifications are as follows:
- Marks from 90 to 100 : Grade A
- Marks from 80 to less than 90 : Grade B
- Marks from 70 to less than 80 : Grade C
- Marks from 60 to less than 70 : Grade D
- Marks from 50 to less than 60 : Grade E 
- Marks below 50 : Grade F
- Marks greater than 100 or less than 0 : Invalid Marks
#include<stdio.h>
int main(){
    int marks;
    printf("Enter your marks:");
    scanf("%d",&marks);
    switch(marks){
        case 90 ... 100:
            printf("Grade A!");
            break;
        case 80 ... 89:
            printf("Grade B!");
            break;
        case 70 ... 79:
            printf("Grade C!");
            break;
        case 60 ... 69:
            printf("Grade D!");
            break;
        case 50 ... 59:
            printf("Grade E!");
            break;
        case 0 ... 49:
            printf("Grade F!");
            break;
        default:
            printf("Invalid Marks");
    }
    return 0;
}

7. Write a menu driven program with the following options:
1.	Factorial of a number
2.	Check Even and Odd.
3.	Area of Circle
4.	Sum of first N natural numbers
5.	Exit
#include<stdio.h>
int main(){
    int k=1;
    while(k){
        int r,n,fact=1;
        printf("\nPlease select an option:\n");
        printf("1. Factorial of a number\n");
        printf("2. Check even and odd\n");
        printf("3. Area of circle\n");
        printf("4. Sum of first n natural numbers\n");
        printf("5. Exit\n");
        scanf("%d",&n);
        switch(n){
            case 1:
                printf("Enter a number:");
                scanf("%d",&r);
                for(int i=1;i<=r;i++)
                    fact=fact*i;
                printf("Factorial is %d.",fact);
                break;
            case 2:
                printf("Enter a number:");
                scanf("%d",&r);
                if(r%2==0)
                    printf("Number is even.");
                else
                    printf("Number id odd.");
                break;
            case 3:
                printf("Enter a radius:");
                scanf("%d",&r);
                printf("Area is %f.",3.14*r*r);
                break;
            case 4:
                printf("Enter a number:");
                scanf("%d",&r);
                int sum=0;
                for(int i=1;i<=r;i++)
                    sum=sum+i;
                printf("Sum is %d.",sum);
                break;
            case 5:
                k=0;
        }
    }
    return 0;
}
8. Write a program to check whether a given character is uppercase alphabet or lowercase alphabet or some other special character, using switch case statement.
#include<stdio.h>
int main(){
    char n;
    printf("Enter a number:");
    scanf("%c",&n);
    switch(n){
        case 'A' ... 'Z':
            printf("Uppercase Alphabet!");
            break;
        case 'a' ... 'z':
            printf("Lowercase Alphabet!");
            break;
        default:
            printf("Some other special Alphabet!");
    }
    return 0;
}
9. Write a program to check whether a given character is vowel or consonant or some other special character, using switch case statement.
#include<stdio.h>
int main(){
    char m;
    printf("Enter a character:");
    scanf("%c",&m);
    if(m>='a' && m<='z')
        m=m-32;
    if(m>='A' && m<='Z')
        switch(m){
            case 'A':
            case 'E':
            case 'I':
            case 'O':
            case 'U':
                printf("Alphabet is vowel.");
                break;
            default:
                printf("Alphabet is consonant.");
        }
    else
        printf("It is not an alphabet.\nIt is any other special symbol.");
    return 0;
}

10. Write a menu driven program with the following options:
1.	Calculate LCM of two numbers
2.	Calculate sum of the digits of a number
3.	Volume of a cuboid
4.	Check whether a given number is prime or not
5.	Exit

#include<stdio.h>
int main(){
    int k=1;
    while(k){
        int n;
        int x,y,z,L,sum=0;
        printf("\nSelect an option:\n");
        printf("1. Calculate LCM of two numbers:\n");
        printf("2. Calculate sum of the digits of a number:\n");
        printf("3. Volume of a cuboid:\n");
        printf("4. Check whether a given number is prime or not:\n");
        printf("5. Exit:\n");
        scanf("%d",&n);
        switch(n){
            case 1:
                printf("Enter first number:");
                scanf("%d",&x);
                printf("Enter second number:");
                scanf("%d",&y);
                for(L=x>y?x:y;L<=x*y;L++){
                    if(L%x==0 && L%y==0){
                        printf("LCM is %d.",L);
                        break;
                    }
                }
                break;
            case 2:
                printf("Enter a number:");
                scanf("%d",&x);
                while(x){
                    sum+=x%10;
                    x/=10;
                }
                printf("Sum is %d.",sum);
                break;
            case 3:
                printf("Enter length of the cuboid:");
                scanf("%d",&x);
                printf("Enter width of the cuboid:");
                scanf("%d",&y);
                printf("Enter height of the cuboid:");
                scanf("%d",&z);
                L=x*y*z;
                printf("Volume is %d.",L);
                break;
            case 4:
                L=0;
                printf("Enter a number:");
                scanf("%d",&x);
                if(x==0 || x==1)
                    L=1;
                for(y=2;y<=x/2;y++){
                    if(x%y==0){
                        L=1;
                        break;
                    }
                }
                if(L==0)
                    printf("Number is prime.");
                else    
                    printf("Number isn't prime.");
                break;
            case 5:
                k=0;
        }
    }
}
